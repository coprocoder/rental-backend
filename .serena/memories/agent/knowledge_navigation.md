# Где что искать — rental-backend

Порядок, а не список: каждый следующий шаг нужен, только если предыдущий не ответил.

1. **`docs/архитектура.md`** — слои, модули, потоки данных, схемы Mermaid. Отвечает на
   «как это устроено» быстрее, чем чтение кода.
2. **`CLAUDE.md` рядом с кодом** — `src/`, `src/modules/`, `src/kernel/`, `src/common/`,
   `src/transport/` и каждый модуль. Загружаются сами, дублировать их нельзя.
3. **`docs/`** — железные правила, БД и инварианты, алгоритмы, API, адаптеры.
4. **`plans/`** — то, что ещё НЕ реализовано: девять модулей, рост данных, нагрузка,
   этапы переезда, найденные дефекты.
5. **`../rental/server/CLAUDE.md`** — как бэкенд работает сегодня, внутри Nuxt.
6. **`../rental-docs/`** — продуктовое: зачем так решили. ⚠️ Не читать пачкой.

## Что где НЕ искать

⚠️ Схема БД — только в `../rental/drizzle/`. Здесь её нет и не будет.
⚠️ Продуктовые решения — только в `../rental-docs/`. Здесь техническое.
⚠️ Состояние готовности — только `../rental-docs/docs/05-работы/TODO.md`. Второй сводки
не заводить: одна уже существовала, разошлась с реальностью на тридцать пунктов и была
принята на веру, потому что выглядела авторитетно.
