# Состояние переезда

Что уже работает в этом сервисе и что ещё живёт в Nuxt. Файл правится **в том же
коммите**, что и работа, иначе он превращается во второй источник правды, расходящийся
с реальностью.

⚠️ Числа в этом файле — про переезд. **О том, что сделано в продукте, судят только по
`../rental-docs/docs/05-работы/TODO.md`.**

Обновлено: 16 сентября 2026.

---

## Готово

| Часть | Где | Проверено |
|---|---|---|
| `kernel`: пул, тенантный контекст, ошибки, часы, outbox, конфигурация, журнал | `src/kernel/` | 8 тестов |
| `transport`: Fastify, единый конверт ошибки, CORS, correlation_id, health check | `src/transport/` | 8 тестов |
| Мягкое завершение (SIGTERM) | `src/main.ts` | — |
| Девять правил границ в CI | `.dependency-cruiser.cjs` | нарушения проверены вручную: правила падают |
| `GET /v1/public/catalog` | `src/modules/catalog/` | **эталон совпал побайтово** (3 формы запроса) |
| Лимиты бронирования: правила отделены от SQL | `src/modules/pricing/` | 7 тестов |
| `shared/` (11 модулей, контракт с фронтом) | `src/shared/` | переехал без правок |

### Что значит «эталон совпал»

Ответы сняты с живого Nuxt-стенда **до** начала работ (`../rental/baseline/`), новый
сервис сравнивался с ними побайтово:

```
tenant=demo                              ✓ идентично
tenant=demo&locale=en                    ✓ идентично   ← локализация названий
tenant=demo&from=2027-07-15T10:00:00Z    ✓ идентично   ← сезонный фильтр, лето
```

---

## Не переехало

**79 эндпоинтов из 80.** По контурам: `admin` 44, `counter` 16, `public` 12, `staff` 4,
`platform` 1, `dev` 1.

Вместе с ними остаётся в `../rental/`:

- **`server/domain/` — 10 200 строк и 322 теста.** Переезжают как есть; это главная
  экономия плана, и трогать их нельзя без отдельного обоснования;
- схема БД и миграции (`drizzle/`) — **дублировать сюда нельзя**, одна из копий станет
  ложью;
- воркер (`scripts/worker.ts`), интеграции уведомлений;
- сессии сотрудников: `requireSession` — единственный по-настоящему связанный с H3 кусок.

---

## Два отличия от Nuxt-версии, сделанные намеренно

**1. `23514` больше не переводится огульно в «не хватает свободных единиц».**

В Nuxt любой CHECK отображался этим текстом. Это уже стоило дефекта: CHECK сработал на
прайсе, а прокат увидел сообщение про склад на экране про деньги (19.23,
`plans/07-НАЙДЕННОЕ.md` п.7). Теперь CHECK ловится на месте вызова, где известно, какое
правило нарушено.

**2. Конверт ошибки — ровно документированный, без обёртки h3.**

Nuxt отдавал клиенту `{ error: true, statusCode, message, data: {…}, stack: […] }` —
**со стеком и абсолютными путями файлов сервера**. Новый сервис отдаёт
`{ error: { code, message, details? } }`.

⚠️ Разбор на фронте это переживает: `app/composables/useApiError.ts` читает `data.error`
первым делом. Закреплено тестом «читает конверт выделенного бэкенда» — он обязан
оставаться зелёным до конца переезда, потому что переключение идёт одной переменной
окружения и откатывается ею же.

---

## Следующий шаг

`plans/06-ЭТАПЫ.md`, этап A — **два блокирующих дефекта** (`plans/07-НАЙДЕННОЕ.md`):
новый вариант с поштучным учётом не продаётся никогда, клиент без email не получает
ссылку. Оба невидимы: система отвечает успехом.

⚠️ Переезжать с ними — значит переносить неработающий продукт. Второй требует
**продуктового решения**, а не технического.
