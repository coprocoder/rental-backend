# Справочник API

Эндпоинты Nitro, файловая маршрутизация: путь файла = путь URL,
суффикс = метод (`quote.post.ts` → `POST /api/v1/public/quote`).

## Общие правила

**Схемы валидации на границах, а не типы роутов.** Nitro проверяет
типы только ответов, никогда тел запросов — поэтому каждое тело
разбирается `valibot`, и невалидное отклоняется до попадания в домен.

**Три уровня доступа:**

| Уровень | Как проверяется |
|---|---|
| публичный | по слагу тенанта; для виджета — публичный ключ + проверка `Origin` |
| стойка / админка | сессия сотрудника (`requireSession`) + право |
| платформа | отдельно |

**Ошибки** — единый формат с кодом:

| Код | HTTP | Когда |
|---|---|---|
| `VALIDATION_FAILED` | 422 | тело не прошло схему |
| `NOT_FOUND` | 404 | нет такого |
| `FORBIDDEN` | 403 | нет права |
| `ITEM_JUST_TAKEN` | 409 | `23P01` — позицию забрали в этот момент |
| `POOL_EXHAUSTED` | 409 | не хватило в пуле |
| `PRICE_CHANGED` | 409 | цена изменилась между расчётом и заказом |
| `HOLD_EXPIRED` | 410 | удержание истекло |
| `LIMIT_EXCEEDED` | 422 | лимит тарифа |
| `OUTSIDE_BUSINESS_HOURS` | 422 | вне часов работы |
| `OUT_OF_SEASON` | 422 | сноуборд на июль |
| `WAITLIST_OFFER_LOST` | 409 | предложение уже забрали или время вышло |
| `TENANT_SUSPENDED` | 403 | прокат отключён |
| `RATE_LIMITED` | 429 | слишком часто |
| `UPSTREAM_UNAVAILABLE` | 502 | внешняя система недоступна |
| `INTERNAL` | 500 | всё остальное |

⚠️ `UPSTREAM_UNAVAILABLE` отделён от «данные неверны» намеренно: если
недоступность Telegram показать как «неверный токен», владелец начнёт
перевыпускать рабочий ключ, а проблема была в сети.

⚠️ Неизвестный и истёкший токен дают **один и тот же** ответ: разные
ответы подсказывали бы перебирающему, что токен угадан верно.


## Публичные (клиент, без входа)

| Метод | Путь | Что делает |
|---|---|---|
| `GET` | `/api/v1/public/agreement/offer` | Текст оферты для показа клиенту |
| `GET` | `/api/v1/public/catalog` | Каталог тенанта: категории, варианты, свободные количества |
| `POST` | `/api/v1/public/max/[slug]` | Вебхук MAX: связывание чата клиента с его заказом (8.4) |
| `POST` | `/api/v1/public/orders` | Создание заказа |
| `GET` | `/api/v1/public/orders/[token]` | Просмотр заказа клиентом по ссылке |
| `POST` | `/api/v1/public/orders/[token]/cancel` | Самостоятельная отмена заказа клиентом |
| `POST` | `/api/v1/public/orders/[token]/confirm` | Подтверждение брони клиентом — одно нажатие по ссылке |
| `POST` | `/api/v1/public/orders/[token]/extend` | Продление аренды клиентом по ссылке |
| `POST` | `/api/v1/public/orders/[token]/sign` | Подписание договора: запрос кода и приём кода |
| `POST` | `/api/v1/public/quote` | Расчёт цены и подбор размера |
| `POST` | `/api/v1/public/telegram/[slug]` | Вебхук Telegram-бота тенанта: связывание чата с клиентом |
| `POST` | `/api/v1/public/waitlist` | Запись в лист ожидания при отсутствии наличия |
| `POST` | `/api/v1/public/waitlist/[token]` | Забрать предложенную позицию по ссылке из уведомления (17.14) |

## Стойка (нужен вход сотрудника)

| Метод | Путь | Что делает |
|---|---|---|
| `GET` | `/api/v1/counter/catalog` | Каталог для выдачи без брони (24-стойка.md) |
| `POST` | `/api/v1/counter/din` | Расчёт рекомендованного DIN по параметрам, названным на стойке |
| `GET` | `/api/v1/counter/history` | Краткая история клиента на экране выдачи |
| `POST` | `/api/v1/counter/incident` | Нештатные ситуации на стойке: неявка, передача, списание, просрочка |
| `POST` | `/api/v1/counter/issue` | Отметить выдачу |
| `GET` | `/api/v1/counter/orders` | Список заказов для стойки |
| `GET` | `/api/v1/counter/orders/[id]` | Заказ для стойки: позиции, которые надо выдать или принять |
| `POST` | `/api/v1/counter/return` | Отметить возврат |
| `GET` | `/api/v1/counter/shift` | Текущая смена филиала со сводкой |
| `POST` | `/api/v1/counter/shift` | Открытие и закрытие смены |
| `POST` | `/api/v1/counter/stocktake` | Ревизия: ведомость пересчёта и применение результата |
| `GET` | `/api/v1/counter/upsell` | Что предложить к заказу на стойке (17.21) |
| `POST` | `/api/v1/counter/upsell` | Добавить допроданную позицию в заказ (17.21) |
| `POST` | `/api/v1/counter/walk-in` | Выдача «с улицы» — человек пришёл без брони |

## Админка (нужен вход и право)

| Метод | Путь | Что делает |
|---|---|---|
| `GET` | `/api/v1/admin/branches` |  |
| `POST` | `/api/v1/admin/branches` | Филиалы: создать, изменить, архивировать |
| `POST` | `/api/v1/admin/bulk` | Массовые операции над инвентарём (13.6) |
| `POST` | `/api/v1/admin/demo` | Демо-данные: создать заново или удалить одной кнопкой |
| `GET` | `/api/v1/admin/export` | Выгрузки в CSV (13.17) |
| `GET` | `/api/v1/admin/fit-rules` |  |
| `POST` | `/api/v1/admin/fit-rules` | Новая версия таблицы подбора (13.10) |
| `GET` | `/api/v1/admin/flags` | Рубильники тенанта (17.19) — чтение |
| `POST` | `/api/v1/admin/flags` | Переключение рубильника прокатом (17.19) |
| `GET` | `/api/v1/admin/integrations` | Состояние интеграций (13.16) |
| `POST` | `/api/v1/admin/integrations` | Сохранение настроек интеграции (13.16) |
| `GET` | `/api/v1/admin/inventory` | Список инвентаря с остатками и будущими бронями (13.4) |
| `POST` | `/api/v1/admin/inventory-qty` | Правка количества инвентаря в один шаг |
| `GET` | `/api/v1/admin/lost-demand` | Отчёт упущенного спроса — основной отчёт продукта |
| `GET` | `/api/v1/admin/offline-reserve` | Резерв под выдачу без связи (10.10) |
| `POST` | `/api/v1/admin/offline-reserve` | Настройка резерва под выдачу без связи (10.10) |
| `GET` | `/api/v1/admin/orders` | Список заказов с курсорной пагинацией и фильтрами |
| `GET` | `/api/v1/admin/orders/[id]` | Карточка заказа (13.3) |
| `POST` | `/api/v1/admin/orders/[id]/action` | Ручные действия над заказом из карточки (13.3) |
| `GET` | `/api/v1/admin/orders/[id]/timeline` | Хронология заказа |
| `GET` | `/api/v1/admin/plan` | Тариф и лимиты (14.4, 14.5) |
| `POST` | `/api/v1/admin/price-rules` | Прайс: создать правило или архивировать |
| `GET` | `/api/v1/admin/pricing` |  |
| `POST` | `/api/v1/admin/privacy` | Запросы субъекта персональных данных: экспорт и удаление |
| `GET` | `/api/v1/admin/reports` | Отчёты владельца (13.19, 17.12) |
| `GET` | `/api/v1/admin/staff` | Сотрудники тенанта (13.13) |
| `POST` | `/api/v1/admin/staff` | Сотрудники: создать, архивировать, разблокировать, сменить PIN |
| `GET` | `/api/v1/admin/texts` |  |
| `POST` | `/api/v1/admin/texts` | Новая редакция текста (13.14) |
| `GET` | `/api/v1/admin/theme` |  |
| `POST` | `/api/v1/admin/theme` | Сохранение темы тенанта (13.15) |
| `GET` | `/api/v1/admin/today` | Экран «Сегодня» — главный экран админки |
| `GET` | `/api/v1/admin/waitlist` | Лист ожидания в админке — с параметрами ожидающих |

## Сессия сотрудника

| Метод | Путь | Что делает |
|---|---|---|
| `POST` | `/api/v1/staff/login` | Вход сотрудника: email и пароль |
| `POST` | `/api/v1/staff/logout` |  |
| `GET` | `/api/v1/staff/me` |  |
| `POST` | `/api/v1/staff/switch` | Быстрое переключение сотрудника по PIN |

## Платформа

| Метод | Путь | Что делает |
|---|---|---|
| `POST` | `/api/v1/platform/register` | Регистрация тенанта — пробный период один сезон |

## Служебные (только в разработке)

| Метод | Путь | Что делает |
|---|---|---|
| `GET` | `/api/v1/dev/tour` | Данные для демонстрационного обхода (экран /tour) |

## Служебное

| Метод | Путь | Что делает |
|---|---|---|
| `GET` | `/api/health` | Проверка связности: БД отвечает, схема на месте, инварианты живы |

Всего эндпоинтов: **68**.

:::note Таблица собрана из кода
Список получен разбором файлов `server/api/**`, описания — из первой
строки комментария каждого эндпоинта. При добавлении эндпоинта эта
таблица требует пересборки, но не ручной правки по памяти.
:::

---

## Добавление нового эндпоинта

Шесть шагов, все обязательные.

```ts
// 1. modules/<модуль>/transport/<имя>.ts — схема на границе
const Body = v.object({ … })                    // valibot, не типы роута

// 2. маршрут
route.post('/v1/admin/things', async (req, reply) => {
  const input = parse(Body, req.body)           // 3. валидация
  const ctx   = await contextOf(req, 'things.manage')   // 4. кто и можно ли
  const result = await createThing(input, ctx, deps)    // 5. service
  return result                                  // 6. форма ответа явная
})
```

⚠️ **Схема valibot не опциональна.** Фреймворк проверяет типы *ответов*, но не тел
запросов — «типизированный fetch» не контракт. Непроверенные данные, дошедшие до домена, —
именно тот баг, ради которого правило существует.

⚠️ **Нормализация — в схеме, один раз.** Телефон канонизируется там же, где валидируется.
`+7 999…` и `8999…` — один человек; ненормализованные они лягут двумя клиентами, потому что
уникальный индекс их не свяжет. Последствия не косметические: лимит активных броней
обходится сменой формата, стойка не находит заказ по телефону, выгрузка по 152-ФЗ отдаёт
часть данных субъекта. Нормализация в схеме означает, что новый эндпоинт **не сможет
забыть** — схема единственный вход.

⚠️ **Никогда не отдавать строки БД как есть.** Ответ собирается явно: иначе внутренние
колонки уезжают клиенту, а переименование колонки ломает фронт.

⚠️ **Право и тариф — две разные проверки.** «Этому сотруднику можно?» и «этот прокат
оплатил?» — разные границы. Проверять по отдельности в каждом обработчике значит однажды
забыть вторую и раздать платную функцию бесплатно. Сначала право, потом тариф: сотруднику
без прав незачем знать тариф проката.

---

## Списки: пагинация и фильтры

Один стиль на весь API:

- **курсорная пагинация** (`?cursor=…&limit=…`), не `offset`: заказы добавляются
  постоянно, и offset даёт дубли и пропуски;
- фильтры — плоские query-параметры;
- сортировка `?sort=-created_at`.

Здесь и нужна динамическая сборка SQL — не ради сокращения кода, а потому что **условия
неизвестны на этапе написания**:

```ts
const w = new WhereBuilder()
w.add('o.tenant_id = $', ctx.tenantId)          // всегда
w.addIf(f.status,   'o.status = $')
w.addIf(f.branchId, 'o.branch_pickup_id = $')
w.addIf(f.cursor,   '(o.created_at, o.id) < ($, $)')
```

⚠️ **Только плейсхолдеры, никакой конкатенации значений** — иначе это SQL-инъекция,
написанная своими руками.

⚠️ **Имена колонок сортировки — через белый список**, а не из строки запроса.
`?sort=` от пользователя не должен попадать в SQL ни при каких условиях.

---

---

## Чего ещё нет

⚠️ **Курсорная пагинация и `WhereBuilder`** описаны выше как правило, но НЕ реализованы:
списки пока отдаются целиком. TODO 19.41.

⚠️ **Ограничение частоты запросов** — только счётчик неудачных попыток входа (10 за
15 минут), и он про подбор пароля, а не про нагрузку. Публичный API не ограничен ничем:
ни от злоупотребления, ни от парсинга каталога конкурентами. TODO 17.28.

⚠️ **OpenAPI не генерируется.** Пока его нет, типы ответов на фронте потеряны — там
`noImplicitAny` выключен именно поэтому (TODO 19.40).
