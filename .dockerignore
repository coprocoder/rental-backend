# ⚠️ Без этого файла в контекст сборки уезжают node_modules (~400 МБ) и
# dist с хоста. Второе опаснее размера: хостовый dist мог быть собран
# другой версией и перекрыл бы результат сборки внутри образа.
node_modules
dist
.git
.env
*.log

# Тесты и фикстуры в образе не нужны: эталон ответов весит больше
# самого сервиса, а прогоняется он на машине разработчика.
test
**/test
*.test.ts

.claude
.serena
docs
plans
