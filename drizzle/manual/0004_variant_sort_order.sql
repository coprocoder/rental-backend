-- Порядок вариантов в размерной сетке.
--
-- ⚠️ Зачем поле, если есть code: сортировка по code даёт для шлемов
-- «hl-l, hl-m, hl-s» — то есть L, M, S. Алфавит имени не равен порядку
-- размера, и для сеток вида S/M/L, XS/S/M или 24.5/25.0 это видно сразу.
-- Порядок размеров — свойство домена, поэтому он задаётся явно, а не
-- выводится из строки кода.
--
-- Идемпотентно: файл может быть прогнан повторно.

ALTER TABLE inventory_variant
  ADD COLUMN IF NOT EXISTS sort_order integer NOT NULL DEFAULT 0;
