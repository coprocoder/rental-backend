/**
 * Публичный контракт модуля платформы: регистрация прокатов, вебхуки
 * мессенджеров.
 *
 * Владение записью: `tenant` (создание), `api_key`.
 */
export { registerPlatformPublic } from './http/public.controller'
