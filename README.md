# rental-backend — выделенный бэкенд

Отдельный сервис на Node (Fastify + Postgres), в который переезжает бэкенд
Nuxt-приложения `../rental/`.

**Переезд идёт.** Прод по-прежнему обслуживает Nuxt; сервис собирается эндпоинт за
эндпоинтом со сверкой с эталоном, снятым до начала работ. Что уже готово —
[`docs/СОСТОЯНИЕ.md`](docs/СОСТОЯНИЕ.md).

```bash
make install
make check    # типы, тесты, границы слоёв
make dev      # нужен Postgres из ../rental/docker-compose.yml (порт 55432)
```

## Как читать

Начать с [`CLAUDE.md`](CLAUDE.md) — слои, модули, правила. Дальше по надобности:

| Документ | О чём | Когда нужен |
|---|---|---|
| [`docs/архитектура.md`](docs/архитектура.md) | слои, модули, потоки данных — со схемами | **первым** |
| [`docs/железные-правила.md`](docs/железные-правила.md) | что нельзя нарушать без обсуждения | до первой правки |
| [`docs/бд-и-инварианты.md`](docs/бд-и-инварианты.md) | схема, ограничения, RLS | при работе с данными |
| [`docs/алгоритмы.md`](docs/алгоритмы.md) | наличие, цены, подбор размера | при работе с доменом |
| [`docs/api.md`](docs/api.md) | контракт, добавление эндпоинта, формат ошибок | при добавлении API |
| [`docs/адаптеры.md`](docs/адаптеры.md) | границы внешних систем | при интеграции |
| [`docs/данные-и-рост.md`](docs/данные-и-рост.md) | рост объёмов в цифрах, секционирование, шардирование | при вопросах про объём |
| [`docs/нагрузка-и-реплики.md`](docs/нагрузка-и-реплики.md) | реплики, балансировка, пулер | при вопросах про нагрузку |
| [`docs/СОСТОЯНИЕ.md`](docs/СОСТОЯНИЕ.md) | что переехало из Nuxt | при вопросах «а это уже есть?» |

⚠️ **Каталог `plans/` удалён 17 сентября 2026.** Планы модулей, слоёв и этапов переезда
выполнены; расчёты роста и нагрузки переехали в `docs/` как справочники; найденные
дефекты живут в `../rental-docs/docs/05-работы/TODO.md` — единственном источнике правды
о том, что сделано.

## Главное в одном абзаце

Бэкенд остаётся **одним сервисом** — модульным монолитом, а не микросервисами: заказ
создаётся одной транзакцией, где участвуют счётчик дней, позиции и `EXCLUDE`-ограничение,
и разрезать это по сервисам значит обменять работающие инварианты на распределённые
транзакции. Масштабируется он **репликами приложения за балансировщиком** (состояние в
Postgres, процесс без памяти между запросами) и лестницей на стороне БД: индексы →
секционирование `pool_day` → архив старых сезонов → выделенная БД для крупного заказчика →
шардирование по `tenant_id` как последнее средство. Ключ шардирования уже есть в каждой
таблице, поэтому последний шаг остаётся возможным, но по расчётам не понадобится.

⚠️ Порядок именно такой, и это не наша выдумка — это стандартная последовательность для
мультитенантных SaaS на Postgres. Пропускать ступени дорого: шардирование, введённое
вместо секционирования, добавляет распределённые транзакции туда, где хватало одной.

## Правила этого репозитория

⚠️ **Цифры в планах измерены, а не оценены.** Каждое число получено скриптом по коду или
запросом к БД; где это прогноз — сказано явно. Перед началом работ перепроверить: код
меняется.

⚠️ **План, который нельзя проверить, — не план.** У каждого этапа есть признак
завершённости, который можно предъявить: зелёная проверка, совпавший ответ API, пройденный
тест.

⚠️ **Приватный проект.** Ничего не публиковать, не пушить на корпоративные хостинги.
